# Клон стороннего расширения-хоста, который лежит рядом только как справочник
# при разработке. Это чужой код — в историю этого репозитория он не тащится.
upstream/

# Единственная зависимость разработки — jsdom для смоук-теста host-профилей
# (tests/hosts.test.mjs). Ставится через `npm i --no-save jsdom`, в браузер не
# попадает и в историю не тащится: расширение остаётся без сборки.
node_modules/
package-lock.json

# Мусор ОС и редакторов
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Локальные заметки, внутренняя документация и скрипты деплоя — не часть расширения
CLAUDE.md
AGENTS.md
UPSTREAM.md
deploy.sh
docs/
.ai/
.claude/
.mcp.json
.serena/
