// Единственное место, где версия задана в JS. Раньше строка жила в трёх местах
// (index.js, editor.js, manifest.json) и уже успела разъехаться с бейджами README.
//
// manifest.json остаётся вторым и последним местом: прочитать его из модуля можно
// только сетевым запросом, а Imaginy не делает ни одного (README, раздел про
// приватность). При релизе правятся ровно два файла: этот и manifest.json.
export const VERSION = '0.6.0';
